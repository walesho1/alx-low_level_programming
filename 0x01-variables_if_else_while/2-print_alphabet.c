#include <time.h>
#include <stdio.h>

/**
 * main - Print the alphabet in lowercase
 *
 * Return: Always 0
 */
int main(void)
{

	char letter;

	for (letter = 'a'; letter < = 'z', + +)
		putchar(letter);

	putchar('\n');

	return (0);
}
